git init
git add <fichier>
git commit  -am "message" (-a: ajouter fichier deja indexé,  -am : message )
git log (quitter 'q')
git checkout <sha>
git checkout master (dernier commit)

git push origin master
